from dataclasses import dataclass

@dataclass
class Config:
    """Es una clase que tiene la configuración 
    del sistema y todo lo disponible para 
    hacer que el sistema funcione."""
    pass 
